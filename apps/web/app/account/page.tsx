"use client";
import { useEffect, useState } from "react";
import { API, readJson } from "../api-client";
type User={id:number;email:string;role:string;status:string};
export default function AccountPage(){const [user,setUser]=useState<User|null>(null);const [error,setError]=useState("");useEffect(()=>{const t=localStorage.getItem("lollipop_access_token");if(!t){location.href="/login";return}fetch(API+"/api/v1/auth/me",{headers:{Authorization:"Bearer "+t}}).then(async r=>{if(!r.ok)throw new Error("Session expired");setUser(await readJson(r))}).catch(()=>{localStorage.removeItem("lollipop_access_token");setError("Your session has expired.");location.href="/login"})},[]);if(!user)return <main className="shell page"><header className="page-header"><p className="eyebrow">ACCOUNT</p><h1>Loading<span>…</span></h1><p>{error}</p></header></main>;return <main className="shell page"><nav className="nav"><a className="brand" href="/" aria-label="Lollipop home"><img src="/icons/lollipop.svg" alt="" /><span>LOLLIPOP</span></a><div className="nav-links"><a href="/discover">Discover</a><a href="/creators">Creators</a></div><button className="ghost-button" onClick={()=>{localStorage.removeItem("lollipop_access_token");location.href="/login"}}>Sign out</button></nav><header className="page-header"><p className="eyebrow">AUTHENTICATED ACCOUNT</p><h1>Your <span>space.</span></h1><p>{user.email}</p></header><section className="account-list"><div className="account-row"><div><p className="eyebrow">ACCOUNT STATUS</p><span>{user.status} · {user.role}</span></div></div><div className="account-row"><div><p className="eyebrow">CREATOR WORKSPACE</p><span>Apply, save your profile and track verification.</span></div><a href="/creators">›</a></div>{user.role==="ADMIN"&&<div className="account-row"><div><p className="eyebrow">ADMIN CONSOLE</p><span>Review creator verification applications.</span></div><a href="/admin/verifications">›</a></div>}</section></main>}
